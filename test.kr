plusOne := fn(in) {
	in + 1
}

myNumber := 100
IO::Println(myNumber)
myNumber := plusOne(myNumber)
IO::Println(myNumber)
myNumber := plusOne(myNumber)
IO::Println(myNumber)
myNumber := plusOne(myNumber)
IO::Println(myNumber)
myNumber := plusOne(myNumber)