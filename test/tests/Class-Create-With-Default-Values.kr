class Human {
	name := "Default"
	age : Number
}

gus := new Human()
gus.name = "Gustav"

IO::Debug(gus.name) // STRING<Gustav>
IO::Debug(gus.age) // NUMBER<0>

gus.age = 20

IO::Debug(gus.age) // NUMBER<20>